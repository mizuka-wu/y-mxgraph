/**
 * 和drawiofile的转换
 * 
 */
import * as Y from 'yjs'

export function parse(xml: string): Y.Array {
    const array = new Y.Array();
    // array.push(xml);
    return array;
}

export function serialize(file: ) {

}